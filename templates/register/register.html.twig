{% extends 'base.html.twig' %}
{% block title %}
    {{ parent() }}inscription
{% endblock %}
{% block body %}

  
    <div id="box-inscription">
        <div class="leftsection">
          <h1 class="text-start">INSCRIPTION</h1>
        
       {{ form(userForm)}}
                      {# {{ form_start(userForm) }}
                  
                        {{ form_row(userForm.nickname) }}
                        
                        {{ form_row(userForm.email) }}
                    
                        {{ form_row(userForm.age) }}
             
                      {{ form_row(userForm.password) }} #}
                       {# {{ form_row(userForm.confirmationpassword) }} #}
                    {# <input type="submit"  name="submit" value="S'inscrire" /> #}
              {# <a href="{{ path('app_login') }}" class="btn btn-secondary">Me connecter</a> #}
                    
                {{ form_end(userForm) }}            
            </div>
        {# </div> #}



           
          {# <input type="text" name="Prenom" placeholder="Pseudo" />
          <input type="text" name="email" placeholder="E-mail" />
          <input type="text" name="Age" placeholder="Age" />
          <input type="password" name="password1" placeholder="Password" />
          <input type="password" name="password2" placeholder="confirmation password" />
          
           #}
          {# <button>
          <a href="{{ path('register') }}">S'inscrire</a>
          </button> #}
          {# </form> #}
        {# </div> #}
        
       <div class="rightsection"> </div>
         </div>
    
{% endblock %}



