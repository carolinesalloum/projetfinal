{% extends 'base.html.twig' %}

{% block title %}Les avis{% endblock %}

{% block body %}
<div class="container">
      <h1 class="title text-center pt-5">Témoignages</h1>
      <h4 class="text-center">Ce que disent nos clients!</h4>
 
      <div class="testimonial">
      {% for comment in comments %}
        <div class="testimonial-info">
          <h4>{{ comment.user.nickname}}</h4>
        </div>
        <div class="testimonial-content">
          <p>
            {{ comment.content }}
          </p>
          <hr>
        </div>
        {% endfor %}
      </div>
 <div class="d-flex justify-content-center py-4 ">
  <a href="{{ path('add_comment')}}" class="button"  >Ajouter mon avis</a></div>
    </div>

  {# <h1 class="text-center pt-5"> Témoingages </h1>
  <h2 class="text-center">Ce que dise notre clients!</h2>
 
  <div class="d-flex justify-content-center py-4 ">
  <a href="{{ path('add_comment')}}" class="btn btn-dark"  >Ajouter mon avis</a></div>
  
   <div class="testimonials-container">
    
    <div class="testimonial">
      {% for comment in comments %}

      <h5>{{ comment.user.nickname}}</h5>
      <p>{{ comment.content }}</p>
      <div class='divider'>
      <hr >
      </div> {% endfor %}
    </div>

 </div>   #}
{% endblock %}


